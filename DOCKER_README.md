# 🐳 Docker Setup для Zelen Restaurant API

## 📋 Что внутри?

Эта конфигурация Docker включает:

- **PHP 8.2** с Apache
- **MySQL 8.0** база данных
- **phpMyAdmin** для управления БД
- **Laravel 10** приложение
- Автоматическая инициализация и миграции

## 🚀 Быстрый старт

### Вариант 1: Makefile (рекомендуется)

```bash
# Установка и запуск
make install

# Или поэтапно:
make start         # Запустить контейнеры
make migrate       # Выполнить миграции
make key-generate  # Сгенерировать APP_KEY
```

### Вариант 2: docker-scripts.sh

```bash
./docker-scripts.sh start
./docker-scripts.sh migrate
```

### Вариант 3: Docker Compose напрямую

```bash
docker-compose up -d
docker-compose exec app php artisan key:generate
docker-compose exec app php artisan migrate
```

## ✅ Проверка работоспособности

```bash
# Запустить скрипт проверки
./healthcheck.sh

# Или проверить вручную
docker-compose ps
curl http://localhost:8000
```

## 🌐 Доступ к сервисам

| Сервис | URL | Описание |
|--------|-----|----------|
| **Laravel API** | http://localhost:8000 | Основное приложение |
| **phpMyAdmin** | http://localhost:8080 | Управление БД |
| **MySQL** | localhost:3306 | База данных |

### Учетные данные MySQL

```
Database: zelen_restaurant
Username: zelen_user
Password: zelen_password
Root Password: root_password
```

## 📦 Структура проекта

```
.
├── Dockerfile                 # PHP/Apache образ
├── docker-compose.yml         # Оркестрация сервисов
├── docker-entrypoint.sh      # Инициализация контейнера
├── docker-scripts.sh         # Утилиты для управления
├── Makefile                  # Упрощенные команды
├── healthcheck.sh            # Проверка работоспособности
├── .dockerignore             # Исключения для Docker
├── .env.docker               # Переменные окружения
└── .devcontainer/
    ├── devcontainer.json     # Конфигурация Codespaces
    └── setup.sh              # Инициализация Codespaces
```

## 🛠️ Команды Makefile

```bash
make help           # Показать справку
make start          # Запустить контейнеры
make stop           # Остановить контейнеры
make restart        # Перезапустить
make rebuild        # Пересобрать образы
make logs           # Показать логи
make shell          # Войти в контейнер app
make mysql          # Открыть MySQL консоль
make migrate        # Выполнить миграции
make seed           # Запустить сидеры
make fresh          # Пересоздать БД (⚠️ удалит данные)
make composer       # Установить зависимости
make cache-clear    # Очистить кеши
make test           # Запустить тесты
make status         # Статус контейнеров
make cleanup        # Удалить всё (⚠️ удалит данные)
```

## 🔧 docker-scripts.sh команды

```bash
./docker-scripts.sh start        # Запустить
./docker-scripts.sh stop         # Остановить
./docker-scripts.sh restart      # Перезапустить
./docker-scripts.sh logs         # Логи
./docker-scripts.sh shell        # Войти в контейнер
./docker-scripts.sh migrate      # Миграции
./docker-scripts.sh cache-clear  # Очистить кеш
./docker-scripts.sh help         # Справка
```

## 🐛 Отладка

### Просмотр логов

```bash
# Все сервисы
make logs

# Только приложение
make logs-app

# Только MySQL
make logs-mysql
```

### Проверка статуса

```bash
make status
# или
docker-compose ps
```

### Вход в контейнер

```bash
make shell
# или
docker-compose exec app bash
```

### Выполнение Artisan команд

```bash
docker-compose exec app php artisan route:list
docker-compose exec app php artisan migrate:status
docker-compose exec app php artisan db:seed
```

## 🔄 Обновление приложения

```bash
# 1. Остановить контейнеры
make stop

# 2. Обновить код (git pull и т.д.)
git pull

# 3. Пересобрать образы
make rebuild

# 4. Выполнить миграции
make migrate

# 5. Очистить кеш
make cache-clear
```

## 🗄️ Работа с базой данных

### Backup

```bash
# Создать backup
make db-backup

# Backup будет сохранен как backup_YYYYMMDD_HHMMSS.sql
```

### Restore

```bash
# Восстановить из backup
make db-restore FILE=backup_20240101_120000.sql
```

### Прямое подключение

```bash
# Через docker-compose
docker-compose exec mysql mysql -u zelen_user -pzelen_password zelen_restaurant

# Или через make
make mysql
```

## ☁️ GitHub Codespaces

Этот проект полностью готов для GitHub Codespaces!

### Запуск в Codespaces

1. Откройте репозиторий на GitHub
2. Нажмите `Code` → `Codespaces` → `Create codespace on main`
3. Дождитесь автоматической установки (2-3 минуты)
4. Готово! Все сервисы автоматически запущены

### Особенности Codespaces

- ✅ Автоматическая установка всех зависимостей
- ✅ Автоматическое выполнение миграций
- ✅ Автоматический проброс портов
- ✅ Готовые расширения VS Code
- ✅ Полностью настроенная среда разработки

## 🔒 Безопасность

### ⚠️ Перед продакшн деплоем:

1. **Измените все пароли** в `docker-compose.yml`
2. **Используйте .env** для секретных данных
3. **Настройте HTTPS**
4. **Ограничьте доступ** к phpMyAdmin
5. **Обновите секреты**
6. **Настройте firewall**

### Рекомендации

- ❌ Не коммитьте `.env` файлы с секретами
- ✅ Используйте сильные пароли
- ✅ Регулярно обновляйте Docker образы
- ✅ Делайте резервные копии БД
- ✅ Ограничивайте доступ к портам

## 📚 Дополнительные файлы

- **[DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md)** - Полная инструкция по развертыванию
- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт в один шаг
- **[DEPLOYMENT_NOTES.md](DEPLOYMENT_NOTES.md)** - Заметки и советы по деплою
- **[API_EXAMPLES.http](API_EXAMPLES.http)** - Примеры API запросов

## 🆘 Решение проблем

### Порты уже заняты

Измените порты в `docker-compose.yml`:

```yaml
services:
  app:
    ports:
      - "8001:80"  # вместо 8000
```

### MySQL не запускается

```bash
# Проверьте логи
make logs-mysql

# Попробуйте пересоздать
make cleanup  # ⚠️ Удалит данные!
make start
```

### Ошибки прав доступа

```bash
docker-compose exec app chown -R www-data:www-data storage bootstrap/cache
docker-compose exec app chmod -R 775 storage bootstrap/cache
```

### APP_KEY не установлен

```bash
make key-generate
```

### Не могу подключиться к MySQL

Убедитесь, что в `.env`:
```
DB_HOST=mysql  # не localhost!
```

## 📊 Мониторинг

### Проверка здоровья сервисов

```bash
# Автоматическая проверка
./healthcheck.sh

# Ручная проверка
curl http://localhost:8000
curl http://localhost:8080
```

### Использование ресурсов

```bash
docker stats
```

## 🎯 Следующие шаги

1. ✅ Запустите `make install`
2. ✅ Проверьте `./healthcheck.sh`
3. ✅ Откройте http://localhost:8000
4. ✅ Протестируйте API используя `API_EXAMPLES.http`
5. ✅ Добавьте сидеры для тестовых данных
6. ✅ Настройте под свои нужды

## 💡 Полезные советы

- Используйте `make help` для списка всех команд
- Логи помогут отладить большинство проблем
- В Codespaces всё работает из коробки
- Делайте backup перед важными операциями
- Используйте volumes для персистентности данных

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `make logs`
2. Запустите проверку: `./healthcheck.sh`
3. Прочитайте [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md)
4. Создайте issue в репозитории

---

**Готово к работе! 🚀**

Для начала работы выполните:
```bash
make install
```

Или для GitHub Codespaces просто откройте репозиторий в Codespaces!
