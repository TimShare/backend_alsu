# 🐳 Развертывание приложения в Docker и GitHub Codespaces

## 📋 Содержание
- [Локальное развертывание с Docker](#локальное-развертывание-с-docker)
- [Развертывание в GitHub Codespaces](#развертывание-в-github-codespaces)
- [Полезные команды](#полезные-команды)
- [Доступ к сервисам](#доступ-к-сервисам)
- [Устранение неполадок](#устранение-неполадок)

---

## 🚀 Локальное развертывание с Docker

### Требования
- Docker Desktop установлен и запущен
- Docker Compose v2.0+

### Шаги для запуска

1. **Клонируйте репозиторий**
   ```bash
   git clone <ваш-репозиторий>
   cd backend_alsu
   ```

2. **Запустите контейнеры**
   ```bash
   docker-compose up -d
   ```

3. **Проверьте статус контейнеров**
   ```bash
   docker-compose ps
   ```

4. **Приложение готово!**
   - Laravel API: http://localhost:8000
   - phpMyAdmin: http://localhost:8080
   - MySQL: localhost:3306

### Остановка контейнеров
```bash
docker-compose down
```

### Полная очистка (включая volumes)
```bash
docker-compose down -v
```

---

## ☁️ Развертывание в GitHub Codespaces

### Метод 1: Через веб-интерфейс GitHub

1. **Откройте репозиторий на GitHub**

2. **Создайте Codespace**
   - Нажмите на кнопку `Code` (зеленая кнопка)
   - Перейдите во вкладку `Codespaces`
   - Нажмите `Create codespace on main`

3. **Автоматический запуск**
   - Codespace автоматически обнаружит `.devcontainer/devcontainer.json`
   - Docker Compose запустится автоматически
   - Выполнится скрипт инициализации `.devcontainer/setup.sh`

4. **Дождитесь завершения setup**
   - В терминале увидите сообщения о выполнении миграций
   - После завершения приложение будет доступно

### Метод 2: Через VS Code Desktop

1. **Установите расширения**
   - GitHub Codespaces (расширение для VS Code)

2. **Откройте Command Palette** (`Cmd+Shift+P` / `Ctrl+Shift+P`)

3. **Выберите** `Codespaces: Create New Codespace`

4. **Следуйте инструкциям на экране**

### Метод 3: Через GitHub CLI

```bash
# Установите GitHub CLI (если еще не установлен)
# macOS
brew install gh

# Linux
sudo apt install gh

# Авторизуйтесь
gh auth login

# Создайте Codespace
gh codespace create --repo <ваш-username>/<ваш-репозиторий>

# Подключитесь к Codespace
gh codespace code
```

---

## 🔧 Полезные команды

### Docker Compose

```bash
# Запуск контейнеров в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f app
docker-compose logs -f mysql

# Остановка контейнеров
docker-compose stop

# Перезапуск контейнеров
docker-compose restart

# Пересборка образов
docker-compose build --no-cache

# Запуск с пересборкой
docker-compose up -d --build
```

### Работа с Laravel внутри контейнера

```bash
# Войти в контейнер приложения
docker-compose exec app bash

# Выполнить Artisan команды
docker-compose exec app php artisan migrate
docker-compose exec app php artisan db:seed
docker-compose exec app php artisan route:list
docker-compose exec app php artisan cache:clear

# Установить зависимости Composer
docker-compose exec app composer install
docker-compose exec app composer update
```

### MySQL команды

```bash
# Подключиться к MySQL
docker-compose exec mysql mysql -u zelen_user -pzelen_password zelen_restaurant

# Экспорт базы данных
docker-compose exec mysql mysqldump -u zelen_user -pzelen_password zelen_restaurant > backup.sql

# Импорт базы данных
docker-compose exec -T mysql mysql -u zelen_user -pzelen_password zelen_restaurant < backup.sql
```

---

## 🌐 Доступ к сервисам

### Локальная разработка

| Сервис | URL | Учетные данные |
|--------|-----|----------------|
| Laravel API | http://localhost:8000 | - |
| phpMyAdmin | http://localhost:8080 | User: `root`<br>Password: `root_password` |
| MySQL | localhost:3306 | Database: `zelen_restaurant`<br>User: `zelen_user`<br>Password: `zelen_password` |

### GitHub Codespaces

При запуске в Codespaces порты будут автоматически проброшены. Вы увидите уведомления:

- **Port 8000**: Laravel приложение
- **Port 8080**: phpMyAdmin
- **Port 3306**: MySQL (обычно используется внутри Codespace)

VS Code покажет всплывающие уведомления для каждого порта. Нажмите "Open in Browser" для доступа.

---

## 🛠️ Устранение неполадок

### Проблема: Контейнер app не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs app

# Пересоберите образ
docker-compose build --no-cache app
docker-compose up -d
```

### Проблема: MySQL не готов

**Решение:**
```bash
# Подождите несколько секунд и проверьте статус
docker-compose ps

# Если MySQL в статусе "unhealthy"
docker-compose restart mysql
```

### Проблема: Ошибка permissions (права доступа)

**Решение:**
```bash
# Войдите в контейнер и исправьте права
docker-compose exec app bash
chown -R www-data:www-data storage bootstrap/cache
chmod -R 775 storage bootstrap/cache
exit
```

### Проблема: База данных не создана

**Решение:**
```bash
# Выполните миграции вручную
docker-compose exec app php artisan migrate --force
```

### Проблема: APP_KEY не сгенерирован

**Решение:**
```bash
docker-compose exec app php artisan key:generate
```

### Проблема: Порты уже заняты

**Решение:**
Измените порты в `docker-compose.yml`:
```yaml
services:
  app:
    ports:
      - "8001:80"  # Вместо 8000
  mysql:
    ports:
      - "3307:3306"  # Вместо 3306
  phpmyadmin:
    ports:
      - "8081:80"  # Вместо 8080
```

### Проблема: Codespace не запускается

**Решения:**
1. Проверьте, что файлы `.devcontainer/` находятся в корне репозитория
2. Проверьте синтаксис в `devcontainer.json`
3. Удалите Codespace и создайте новый
4. Проверьте логи в терминале Codespace

---

## 📝 Структура Docker файлов

```
.
├── Dockerfile                    # Основной образ приложения
├── docker-compose.yml           # Оркестрация всех сервисов
├── docker-entrypoint.sh        # Скрипт инициализации для Docker
├── .dockerignore               # Исключения для Docker build
└── .devcontainer/
    ├── devcontainer.json       # Конфигурация для Codespaces
    └── setup.sh                # Скрипт инициализации для Codespaces
```

---

## 🔐 Безопасность

> ⚠️ **ВАЖНО**: Перед деплоем в продакшн:

1. Измените все пароли в `docker-compose.yml`
2. Используйте `.env` файл для секретных данных
3. Не коммитьте `.env` в Git
4. Настройте HTTPS
5. Ограничьте доступ к phpMyAdmin

---

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Laravel Documentation](https://laravel.com/docs)
- [GitHub Codespaces Documentation](https://docs.github.com/en/codespaces)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

---

## 💡 Советы

1. **Используйте docker-compose logs** для отладки проблем
2. **Регулярно обновляйте образы**: `docker-compose pull`
3. **Делайте backup базы данных** перед важными изменениями
4. **Используйте volumes** для персистентного хранения данных
5. **В Codespaces** все изменения сохраняются автоматически

---

## ✅ Checklist после запуска

- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] MySQL доступен и здоров
- [ ] Миграции выполнены
- [ ] API отвечает на http://localhost:8000
- [ ] phpMyAdmin доступен на http://localhost:8080
- [ ] Можно подключиться к базе данных

---

Если у вас возникли вопросы или проблемы, создайте issue в репозитории!
