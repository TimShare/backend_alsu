# 📝 Заметки по развертыванию

## 🎯 Что было создано

### Основные файлы Docker
1. **Dockerfile** - образ PHP 8.2 с Apache и всеми необходимыми расширениями
2. **docker-compose.yml** - оркестрация трех сервисов:
   - `app` - Laravel приложение (порт 8000)
   - `mysql` - База данных MySQL 8.0 (порт 3306)
   - `phpmyadmin` - Веб-интерфейс для MySQL (порт 8080)
3. **.dockerignore** - исключения для Docker build
4. **docker-entrypoint.sh** - скрипт инициализации для Docker контейнера

### GitHub Codespaces
1. **.devcontainer/devcontainer.json** - конфигурация для Codespaces
2. **.devcontainer/setup.sh** - скрипт инициализации для Codespaces

### Утилиты разработки
1. **docker-scripts.sh** - bash скрипт с удобными командами
2. **Makefile** - альтернатива docker-scripts с более короткими командами
3. **API_EXAMPLES.http** - примеры API запросов для REST Client

### Конфигурационные файлы
1. **.env.docker** - переменные окружения для Docker
2. **.env.codespaces** - переменные окружения для Codespaces
3. **.gitignore** - исключения для Git

### CI/CD
1. **.github/workflows/docker-build.yml** - GitHub Actions для автоматического тестирования

### Документация
1. **DOCKER_DEPLOYMENT.md** - полная инструкция по развертыванию
2. **QUICKSTART.md** - быстрый старт
3. **DEPLOYMENT_NOTES.md** - этот файл

---

## 🚀 Быстрый старт

### Локально
```bash
# Используйте docker-scripts.sh
./docker-scripts.sh start

# Или Makefile
make start

# Или напрямую docker-compose
docker-compose up -d
```

### В GitHub Codespaces
1. Откройте репозиторий на GitHub
2. Code → Codespaces → Create codespace on main
3. Дождитесь автоматической настройки

---

## 🔧 Переменные окружения

### Для локальной разработки с Docker
Используйте `.env.docker` или создайте свой `.env`:

```bash
cp .env.docker .env
# Или
cp .env.example .env
```

**Важно:** Убедитесь, что `DB_HOST=mysql` (имя сервиса из docker-compose)

### Для GitHub Codespaces
Используйте `.env.codespaces`:

```bash
cp .env.codespaces .env
```

---

## 📊 Порты

| Сервис | Локально | Codespaces |
|--------|----------|------------|
| Laravel API | 8000 | Автопроброс |
| phpMyAdmin | 8080 | Автопроброс |
| MySQL | 3306 | Автопроброс |

---

## 🗄️ База данных

### Учетные данные
- **Database**: `zelen_restaurant`
- **User**: `zelen_user`
- **Password**: `zelen_password`
- **Root Password**: `root_password`

### Миграции
```bash
# Через Makefile
make migrate

# Через docker-scripts
./docker-scripts.sh migrate

# Напрямую
docker-compose exec app php artisan migrate
```

### Backup и Restore
```bash
# Backup
make db-backup

# Restore
make db-restore FILE=backup.sql
```

---

## 🛠️ Полезные команды

### Makefile команды
```bash
make help          # Справка
make start         # Запустить
make stop          # Остановить
make restart       # Перезапустить
make rebuild       # Пересобрать
make logs          # Логи
make shell         # Войти в контейнер
make mysql         # MySQL консоль
make migrate       # Миграции
make cache-clear   # Очистить кеш
make test          # Тесты
make status        # Статус
```

### docker-scripts.sh команды
```bash
./docker-scripts.sh help     # Справка
./docker-scripts.sh start    # Запустить
./docker-scripts.sh logs     # Логи
./docker-scripts.sh shell    # Войти в контейнер
```

---

## 🔍 Отладка

### Проверка логов
```bash
# Все сервисы
docker-compose logs -f

# Только app
docker-compose logs -f app

# Только MySQL
docker-compose logs -f mysql
```

### Проверка статуса
```bash
docker-compose ps
```

### Вход в контейнер
```bash
docker-compose exec app bash
```

### Проверка подключения к MySQL
```bash
docker-compose exec mysql mysql -u zelen_user -pzelen_password -e "SHOW DATABASES;"
```

---

## 🔒 Безопасность

### Перед продакшном
1. Измените пароли в `docker-compose.yml`
2. Используйте секреты для чувствительных данных
3. Настройте HTTPS
4. Ограничьте доступ к phpMyAdmin
5. Используйте `.env` файл вместо прямых значений

### Рекомендации
- Не коммитьте `.env` файл
- Используйте сильные пароли
- Регулярно обновляйте образы
- Делайте backup базы данных

---

## 📦 Volumes

### MySQL Data
Данные MySQL хранятся в volume `mysql-data` и сохраняются между перезапусками.

### Удаление volumes
```bash
# Удалить все (ВНИМАНИЕ: потеряете данные!)
docker-compose down -v

# Или через Makefile
make cleanup
```

---

## 🌐 Доступ к сервисам

### После запуска откройте:
- **API**: http://localhost:8000
- **API Health**: http://localhost:8000/api/health
- **phpMyAdmin**: http://localhost:8080

### В Codespaces
VS Code автоматически откроет порты и покажет уведомления

---

## 🧪 Тестирование

### Запуск тестов
```bash
make test
# или
docker-compose exec app php artisan test
```

### Ручное тестирование API
Используйте `API_EXAMPLES.http` с REST Client расширением для VS Code

---

## 📝 Checklist перед коммитом

- [ ] Все контейнеры запускаются успешно
- [ ] Миграции выполняются без ошибок
- [ ] API отвечает на запросы
- [ ] phpMyAdmin доступен
- [ ] Тесты проходят
- [ ] Логи не содержат критических ошибок
- [ ] `.env` не добавлен в git

---

## 🆘 Частые проблемы

### Порты заняты
Измените порты в `docker-compose.yml`

### MySQL не запускается
```bash
docker-compose logs mysql
docker-compose down -v  # ВНИМАНИЕ: удалит данные
docker-compose up -d
```

### Ошибки прав доступа
```bash
docker-compose exec app chown -R www-data:www-data storage bootstrap/cache
docker-compose exec app chmod -R 775 storage bootstrap/cache
```

### APP_KEY не установлен
```bash
make key-generate
# или
docker-compose exec app php artisan key:generate
```

---

## 📚 Дополнительные ресурсы

- [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md) - Полная инструкция
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [API_EXAMPLES.http](API_EXAMPLES.http) - Примеры API

---

## ✅ Что дальше?

1. Запустите приложение локально для проверки
2. Протестируйте в GitHub Codespaces
3. Настройте переменные окружения под ваши нужды
4. Добавьте сидеры для тестовых данных
5. Настройте CI/CD пайплайн
6. Подготовьте к продакшн деплою

---

**Готово! Ваше приложение готово к развертыванию! 🎉**
